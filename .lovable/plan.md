## Plan: Import Sync Hub Dashboard

Copy the uploaded previous Lovable project into this workspace, replacing the current blank template.

### Steps
1. Verify the archive has no `.git` metadata (already confirmed clean).
2. Rsync `/tmp/sync_hub/` into `/dev-server/` excluding `.git`, overwriting existing template files (`src/routes/__root.tsx`, `src/routes/index.tsx`, `package.json`, `vite.config.ts`, `tsconfig.json`, `components.json`, `AGENTS.md`, `eslint.config.js`, `bunfig.toml`, `bun.lock`, `public/`).
3. Bring over full `src/` tree: routes (activity, context, design, features, mappings, planner, review, settings, simulate, plus `.mcp` and `.well-known` server routes), `components/` (including `crosssync/` and `ui/`), `data/`, `hooks/`, `lib/` (including `mcp/`), and any styles/config files.
4. Remove the placeholder `src/routes/index.tsx` content by virtue of overwrite.
5. Let the harness install dependencies from the new `package.json` and rebuild; auto-generated `routeTree.gen.ts` will be regenerated by the router plugin.

### Notes
- No new code will be written — this is a straight file port.
- The `.lovable/` folder from the archive will be skipped (managed per project).
- If build errors surface after import, they'll be triaged separately.
